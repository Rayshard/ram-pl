	PUSH<I>(5);
	CALL(%FACTORIAL, 3, 4);
	MOV<I>([SP-3], $0);
	POP<I>(1);
	
	STORE<B>(72, 69, 108, 108, 79, 10, $1);
	PRINT($1, 6);

	PUSH<B>(72, 69, 108, 108, 79, 10);
	COMPARE({$1}, [SP-5], 6, [+]);
	HALT;

%FACTORIAL;
	MOV<I>([SP-3], $0);
	MOV<I>(1, $1);

%LOOP;
	EQ<IIB>($0, 0, $2);
	JUMPT(%END_LOOP, $2);
	MUL<III>($0, $1, $1);
	SUB<III>($0, 1, $0);
	JUMP(%LOOP);
%END_LOOP;
	PUSH<I>($1);
	RET(4);