	PUSH<I>(5);
	CALL(%FACTORIAL, 4);
	
	STORE<B>(72, 69, 108, 108, 79, 10, [$SP+1]);
	PRINT([$SP-3], 6);
	
	PUSH<B>(72, 69, 108, 108, 79, 10);
	COMPARE({[$SP-9]}, [$SP-5], 6, [$SP+1]);
	HALT;

%FACTORIAL;
	PUSH<I>(1); #Accumulator
	PUSH<I>(0); #Check
%LOOP;
	EQ<IIB>([$FP], 0, [$FP+8]);
	JUMPT(%END_LOOP, [$FP+8]);
	MUL<III>([$FP], [$FP+4], [$FP+4]);
	SUB<III>([$FP], 1, [$FP]);
	JUMP(%LOOP);
%END_LOOP;
	PUSH<I>([$FP+4]);
	RET(4);